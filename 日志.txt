该文件记录每次代码修改以及心得

2024/11/23
1) P1_3和P13不是一个东西 数值不一样 P1_3在枚举里面 值为0x13 而P13为引脚 数值只有0或1（其他引脚以此类推）

2024/11/24
1) 头文件声明全局变量不可以（extern uint16 ****）这样声明 要（extern unsigned int ****）这样声明

2024/11/26
1) 遇到和逻辑不符的奇怪现象时 且实在找不到代码逻辑错误时 检查代码有没有哪里拼写错误 有时候不明显的拼写错误此编译器不会报错
2）全局变量可能会出现变量的值随时变化的情况 若出现此类情况 加上volatile关键字看能否解决问题
   注意：对于volatile类型的变量，系统每次用到他的时候都是直接从对应的内存当中提取，**而不会利用cache（缓存）当中的原有数值，
   **以适应它的未知何时会发生的变化，系统对这种变量的处理不会做优化——显然也是因为它的数值随时都可能变化的情况
   可是如果你大量使用volatile的话，没有优化，就会降低程序运行的效率。

2024/11/28
1) 定时器234优先级默认最低
2）定时器中断服务函数无需清除标志位 自动重载 硬件擦除 

2024/12/04
1）所有变量统一定义在一个Data.c文件 Data.h文件有大量全局变量声明（这样处理是否会有问题暂时未知）

2024/12/16
1）尽量不用定时器2中断（主循环只有屏幕显示， 使用定时器2中断屏幕刷新极慢， 原因暂时未知）

2025/1/6
1）定时器中断函数内不要用printf打印 程序会卡死














